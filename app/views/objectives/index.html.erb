<%= link_to 'New Objective', new_objective_path, class: "btn btn-success pull-right" %>
<h3>Objectives</h3>
  
<div class="row flex-row">
  <% @objectives.each do |objective| %>
    <div class="col-md-6 col-lg-4">
      <%= render objective %>
    </div>
  <% end %>
</div>