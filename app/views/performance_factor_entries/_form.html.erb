<%= form_for(@performance_factor_entry) do |f| %>
  <% if @performance_factor_entry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@performance_factor_entry.errors.count, "error") %> prohibited this performance_factor_entry from being saved:</h2>

      <ul>
      <% @performance_factor_entry.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :performance_factor_id %><br>
    <%= f.text_field :performance_factor_id %>
  </div>
  <div class="field">
    <%= f.label :measure_report_id %><br>
    <%= f.text_field :measure_report_id %>
  </div>
  <div class="field">
    <%= f.label :data %><br>
    <%= f.text_field :data %>
  </div>
  <div class="field">
    <%= f.label :comments %><br>
    <%= f.text_area :comments %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
