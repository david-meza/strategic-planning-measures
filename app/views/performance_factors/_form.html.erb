<%= form_for(@performance_factor) do |f| %>
  <% if @performance_factor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@performance_factor.errors.count, "error") %> prohibited this performance_factor from being saved:</h2>

      <ul>
      <% @performance_factor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :performance_measure_id %><br>
    <%= f.text_field :performance_measure_id %>
  </div>
  <div class="field">
    <%= f.label :label_text %><br>
    <%= f.text_field :label_text %>
  </div>
  <div class="field">
    <%= f.label :field_type %><br>
    <%= f.text_field :field_type %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
